import { storeName } from 'day68-ember-two-day/helpers/store-name';
import { module, test } from 'qunit';

module('Unit | Helper | store name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = storeName([42]);
  assert.ok(result);
});
