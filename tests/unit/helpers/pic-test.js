import { pic } from 'day68-ember-two-day/helpers/pic';
import { module, test } from 'qunit';

module('Unit | Helper | pic');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pic([42]);
  assert.ok(result);
});
