import { discussPopularity } from 'day68-ember-two-day/helpers/discuss-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | discuss popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = discussPopularity([42]);
  assert.ok(result);
});
